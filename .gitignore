node_modules/
.next/
out/
build/

# Secrets — never commit. This repo is public.
.env
.env.local
.env*.local
*.pem
*-service-account*.json

.vercel
*.log
.DS_Store
Thumbs.db
payments-backup-*.json

# Readable dumps of what the model actually receives, for comparing
# prompt versions by eye. Regenerated on demand; never part of a build.
PROMPT-*.txt
DIRECTOR-*.txt
